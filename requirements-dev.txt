# Test-only dependencies. Not installed in the production image.
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.25.0
anyio==4.7.0
# Hard per-test timeout so a streaming test can never hang a CI job.
pytest-timeout==2.3.1
